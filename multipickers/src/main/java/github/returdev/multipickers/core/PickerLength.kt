package github.returdev.multipickers.core

/**
 * Enumeration representing the length of the visible picker area.
 *
 * @property value The numeric value associated with the picker length.
 */
enum class PickerLength(val value : Int) {
    SHORT(3),
    LONG(5)
}